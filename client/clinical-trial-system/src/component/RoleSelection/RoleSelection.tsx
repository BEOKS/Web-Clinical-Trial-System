import {Button} from "@mui/material";
const RoleSelection=()=>{
    return (
        <div>
            <Button data-testid={'pi-button'}>PI</Button>
            <Button data-testid={'reviewer-button'}></Button>
        </div>
    )
}
export default RoleSelection;